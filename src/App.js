import Loading from './components/Loading';
import styled from 'styled-components';

function App() {
  return (
    <div style={{ padding: '2rem' }}>
      <Loading />
    </div>
  );
}
export default App;
